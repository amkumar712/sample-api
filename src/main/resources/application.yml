server:
  port: 8081

spring:
  application:
    name: sample-jenkins-docker
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/foto
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
